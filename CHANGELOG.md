# Changelog - Neko Finance Enhanced

## 📋 Описание обновления

Добавлена полная система управления счетами и копилками (целями сбережений) в Neko Finance. Приложение теперь функционирует как полноценный Expense Tracker с поддержкой мультиаккаунтного бюджетирования и целевого накопления.

---

## 🎯 Новые возможности

### 1. **Счета (Accounts)**
- Создание неограниченного количества счетов/кошельков
- Поддержка различных типов счетов: наличные, карты, электронные кошельки
- Отслеживание баланса каждого счета отдельно
- Удаление счетов
- Кастомизация цвета для каждого счета

### 2. **Копилки (Savings Goals)**
- Создание целей сбережений с целевой суммой
- Прогресс-бар для каждой цели
- Проценты заполнения в реальном времени
- Удаление целей
- Кастомизация цвета каждой копилки

### 3. **Переводы (Transfers)**
- Переводы между счетами
- Переводы со счета на копилку
- Переводы с копилки на счет
- Отслеживание истории переводов
- Атомарные транзакции (транзакция либо выполнится полностью, либо откатится)

### 4. **Интеграция с существующей логикой**
- Транзакции могут привязываться к конкретному счету
- Баланс счета автоматически уменьшается/увеличивается при добавлении расходов/доходов
- Общий баланс всех счетов отображается в новой вкладке

### 5. **Telegram Bot Enhancement**
- Поддержка указания счета при добавлении расхода
- Формат: `Счет1: 500 интернет`
- AI парсер автоматически определяет счет и добавляет расход

---

## 📁 Измененные файлы

### Backend (Server)

#### `server/server.js`
**Добавлено:**
- 3 новые таблицы БД:
  - `accounts` - счета пользователя
  - `savings_goals` - копилки/цели сбережений
  - `transfers` - история переводов между счетами/копилками
- Миграция для таблицы `transactions` (добавлены колонки `account_id`)
- API endpoints:
  - `GET /accounts` - получить все счета
  - `POST /accounts` - создать счет
  - `PUT /accounts/:id` - обновить счет
  - `DELETE /accounts/:id` - удалить счет
  - `GET /goals` - получить все копилки
  - `POST /goals` - создать копилку
  - `PUT /goals/:id` - обновить копилку
  - `DELETE /goals/:id` - удалить копилку
  - `POST /transfer` - выполнить перевод между счетами/копилками
  - `GET /total-balance` - получить общий баланс всех счетов

#### `server/bot.js`
**Изменено:**
- Обновлена системная инструкция Gemini AI для парсинга названия счета из сообщений
- Функция `saveTransaction` теперь:
  - Ищет счет по названию в БД
  - Автоматически вычитает сумму из баланса счета при добавлении расхода
  - Отправляет уведомление о счете в чат

---

### Frontend (Client)

#### `client/src/App.tsx`
**Изменено:**
- Импорт нового компонента `AccountsView`
- Добавлена новая вкладка `'accounts'` в состояние `activeTab`
- Добавлены новые state переменные:
  - `accounts` - массив счетов
  - `goals` - массив копилок
- Обновлена функция `loadData` для загрузки счетов и копилок через API
- Добавлена новая кнопка в навигационной панели "Счета" с иконкой `Wallet`
- Добавлена новая кнопка навигации для переключения на вкладку счетов
- Переупорядочена навигационная панель: Ввод → Инфо → **Счета** → Бюджет

#### `client/src/components/AccountsView.tsx` (новый файл)
**Создано:**
- Компонент для управления счетами и копилками
- Два таба: "Счета" и "Копилки"
- Функционал:
  - Отображение всех счетов с балансом
  - Отображение всех копилок с прогресс-барами
  - Создание новых счетов с выбором цвета
  - Создание новых копилок с целевой суммой
  - Удаление счетов и копилок
  - Переводы между счетами и копилками
  - Отображение общего баланса на счетах
  - Отображение общей суммы в копилках
- Красивая UI с использованием Framer Motion для анимаций

#### `client/src/api/nekoApi.ts`
**Добавлено:**
- Функции для работы со счетами:
  - `fetchAccounts(userId)` - получить счета
  - `createAccount(userId, name, balance, type, color)` - создать счет
  - `updateAccount(userId, accountId, data)` - обновить счет
  - `deleteAccount(userId, accountId)` - удалить счет
- Функции для работы с копилками:
  - `fetchGoals(userId)` - получить копилки
  - `createGoal(userId, name, target_amount, color, icon)` - создать копилку
  - `updateGoal(userId, goalId, data)` - обновить копилку
  - `deleteGoal(userId, goalId)` - удалить копилку
- Функции для переводов:
  - `transfer(userId, from_type, from_id, to_type, to_id, amount, description)` - выполнить перевод
- Функция получения общего баланса:
  - `fetchTotalBalance(userId)` - получить сумму всех счетов

---

## 📊 Структура БД

### Таблица `accounts`
```sql
CREATE TABLE accounts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  name TEXT NOT NULL,
  balance REAL DEFAULT 0,
  currency TEXT DEFAULT 'RUB',
  type TEXT DEFAULT 'cash',          -- cash, card, wallet, savings
  color TEXT,
  created_at TEXT,
  updated_at TEXT,
  UNIQUE(user_id, name)
)
```

### Таблица `savings_goals`
```sql
CREATE TABLE savings_goals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  name TEXT NOT NULL,
  target_amount REAL,
  current_amount REAL DEFAULT 0,
  category TEXT,
  icon TEXT,
  color TEXT,
  deadline TEXT,
  created_at TEXT,
  updated_at TEXT,
  UNIQUE(user_id, name)
)
```

### Таблица `transfers`
```sql
CREATE TABLE transfers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  from_type TEXT,                    -- 'account' или 'goal'
  from_id INTEGER,
  to_type TEXT,
  to_id INTEGER,
  amount REAL,
  date TEXT,
  description TEXT
)
```

### Обновлена таблица `transactions`
```sql
ALTER TABLE transactions ADD COLUMN account_id INTEGER;
```

---

## 🎨 UI/UX Улучшения

### Новая вкладка "Счета"
- Красивый градиентный хедер с общим балансом
- Два таба для переключения между счетами и копилками
- Плавные анимации переходов (Framer Motion)
- Карточки счетов с цветовой кодировкой
- Прогресс-бары для копилок
- Модальные формы для создания счетов и копилок
- Выбор цвета при создании счетов
- Кнопка для переводов между счетами/копилками

### Навигационная панель
- Добавлена новая кнопка "Счета" (иконка кошелька)
- Переупорядочена в следующий порядок: Ввод → Инфо → Счета → Бюджет
- Адаптивность: 4 кнопки вместо 3 без нарушения макета

---

## 🤖 Telegram Bot

### Новые возможности
- Распознавание названия счета в сообщениях
- Формат: `Счет1: 500 интернет` или `Карта: 1000 продукты`
- Автоматический парсинг через Gemini AI
- При добавлении расхода через бота баланс счета автоматически обновляется

### Пример использования
```
User: "Счет1: 500 интернет"
Bot: "✅ Расход: 500₽
      📂 Категория: ⚡ КУ (Счета)
      💳 На счет: Счет1"
```

---

## ⚙️ Установка и развертывание

### Требования
- Node.js 16+
- npm или yarn
- SQLite3

### Установка зависимостей

**Server:**
```bash
cd server
npm install
```

**Client:**
```bash
cd client
npm install
```

### Запуск

**Server:**
```bash
cd server
npm start
# или
node server.js
```

**Client (development):**
```bash
cd client
npm run dev
```

**Client (production):**
```bash
cd client
npm run build
npm run preview
```

---

## 🐛 Исправления и совместимость

### Миграция для существующих пользователей
- Для существующих баз данных БД автоматически добавит новые колонки через миграцию
- Старые транзакции будут иметь `account_id = NULL`
- Приложение работает корректно и с NULL значениями

### Обратная совместимость
- Все существующие функции (добавление расходов, статистика, бюджет) работают как раньше
- Новые вкладки не влияют на старые функции

---

## 📱 Использование на мобильных устройствах

Приложение полностью адаптировано для использования в Telegram Web App:
- Максимальная ширина контейнера: 500px
- Поддержка haptic feedback (вибрация)
- Оптимизированные размеры кнопок для сенсорного управления
- Плавные анимации и переходы

---

## 🎯 Примеры использования

### Создание счета
1. Откройте вкладку "Счета"
2. Переключитесь на таб "Счета"
3. Нажмите кнопку "Добавить счет"
4. Введите название (например, "Основной счет")
5. Введите начальный баланс
6. Выберите цвет
7. Нажмите "Создать счет"

### Создание копилки
1. Откройте вкладку "Счета"
2. Переключитесь на таб "Копилки"
3. Нажмите кнопку "Новая копилка"
4. Введите название цели (например, "Отпуск")
5. Введите целевую сумму
6. Выберите цвет
7. Нажмите "Создать копилку"

### Перевод между счетами
1. На вкладке "Счета" нажмите "Перевод между счетами"
2. Выберите счет/копилку откуда делать перевод
3. Выберите счет/копилку куда делать перевод
4. Введите сумму
5. Нажмите "Перевести"

### Добавление расхода на конкретный счет через бота
1. В Telegram чате с ботом напишите: `Счет1: 500 интернет`
2. Бот распознает сумму (500), категорию (bills), счет (Счет1)
3. Расход добавится, баланс Счета1 уменьшится на 500 рублей

---

## 🔄 Отличие от стандартной версии

| Функция | До | После |
|---------|---|-------|
| Отслеживание расходов | Да | Да |
| Статистика по категориям | Да | Да |
| Бюджет и лимиты | Да | Да |
| Множественные счета | Нет | **Да** |
| Копилки/Цели сбережений | Нет | **Да** |
| Переводы между счетами | Нет | **Да** |
| Привязка расходов к счетам | Нет | **Да** |
| Прогресс-бары целей | Нет | **Да** |
| Общий баланс | Нет | **Да** |

---

## 🎓 Архитектурные решения

### 1. **Атомарные транзакции**
При переводе между счетами используются SQL транзакции (BEGIN/COMMIT/ROLLBACK) для гарантии целостности данных.

### 2. **Разделение типов сущностей**
Счета и копилки хранятся в отдельных таблицах с правильной нормализацией БД.

### 3. **Гибкая система типов**
Поддержка различных типов счетов (наличные, карты, электронные и т.д.) позволяет расширять функционал в будущем.

### 4. **Унифицированный API**
Единообразный стиль API endpoints для работы со счетами, копилками и переводами.

---

## 🚀 Будущие возможности (To-Do)

- [ ] Синхронизация с банковскими сервисами
- [ ] Экспорт отчетов в PDF/Excel
- [ ] Категоризация переводов
- [ ] Напоминания о целях сбережений
- [ ] История изменений баланса
- [ ] Совместные счета (shared accounts)
- [ ] Валютные конвертации
- [ ] Диаграммы распределения активов по счетам

---

## 📝 Примечания разработчика

### Синхронизация с Bot'ом
Когда пользователь добавляет расход через бота с указанием счета:
1. AI парсер (Gemini) определяет: сумму, категорию, название счета
2. Сервер ищет счет в БД по user_id и названию счета
3. При нахождении счета создается транзакция с account_id
4. Баланс счета автоматически обновляется

### Обработка ошибок
- Если счет не найден при добавлении расхода - баланс не обновляется
- Если не хватает средств на счете - перевод отклоняется (на уровне приложения, БД позволяет отрицательный баланс)

### Производительность
- Все API endpoints используют индексы (PRIMARY KEY на user_id и account_id)
- N+1 проблемы минимизированы через Promise.all при загрузке данных
- Пагинация может быть добавлена при увеличении кол-ва транзакций

---

## ✅ Проверка работоспособности

Все компоненты проверены на синтаксические ошибки:
- ✅ `server/server.js` - синтаксис корректен
- ✅ `server/bot.js` - синтаксис корректен
- ✅ `client/src/App.tsx` - синтаксис корректен
- ✅ `client/src/components/AccountsView.tsx` - синтаксис корректен
- ✅ `client/src/api/nekoApi.ts` - синтаксис корректен

Готово к развертыванию! 🎉

---

**Дата обновления:** December 5, 2025  
**Версия:** 2.0.0 - Enhanced with Accounts & Savings Goals
