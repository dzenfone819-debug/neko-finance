# 🚀 Инструкция по развертыванию на сервере

## Перед началом
Убедитесь что у вас есть доступ по SSH на сервер и что там установлены:
- Git
- Docker и Docker Compose
- Node.js (если запускаете без Docker)

## Процесс развертывания

### Способ 1: С помощью Docker Compose (рекомендуется)

#### На вашем локальном компьютере:

```bash
# 1. Убедитесь что все изменения закоммичены
cd c:\Bots\neko-finance
git status  # должно быть "nothing to commit"

# 2. Залейте изменения на GitHub
git push origin main
```

#### На сервере:

```bash
# 1. Перейдите в директорию проекта (или создайте её)
cd /path/to/neko-finance

# 2. Если это первый раз или хотите очистить всё:
# ВНИМАНИЕ: Это удалит все контейнеры, образы и данные!
docker compose down -v
rm -rf node_modules client/node_modules client/dist

# 3. Обновите код из GitHub
git pull origin main

# 4. Пересоберите образы и запустите контейнеры
docker compose up --build -d

# 5. Проверьте что контейнеры запущены
docker compose ps

# 6. Проверьте логи
docker compose logs -f
```

### Способ 2: Развертывание без Docker (если запускаете Node.js напрямую)

#### На сервере:

```bash
# 1. Перейдите в директорию проекта
cd /path/to/neko-finance

# 2. Полная очистка (опционально)
# Удалите все старые файлы которые больше не нужны:
git clean -fd  # удалит untracked files
git reset --hard HEAD  # вернёт к последнему коммиту

# 3. Обновите код
git pull origin main

# 4. Установите зависимости (если не установлены)
cd server && npm install && cd ..
cd client && npm install && npm run build && cd ..

# 5. Перезагрузите приложение (если используете PM2 или systemd)
pm2 restart neko-finance  # если используете PM2
# или
sudo systemctl restart neko-finance  # если используете systemd
```

### Способ 3: Полная очистка + свежее развертывание

Если хотите убедиться что абсолютно всё заменено без остатков старого кода:

```bash
# На сервере:

# 1. Остановите контейнеры/приложение
docker compose down  # для Docker Compose
# или
pm2 stop neko-finance  # для PM2

# 2. Удалите ВСЮ директорию проекта
rm -rf /path/to/neko-finance

# 3. Заново клонируйте репозиторий
git clone https://github.com/dzenfone819-debug/neko-finance.git /path/to/neko-finance

# 4. Перейдите в директорию
cd /path/to/neko-finance

# 5. Запустите проект
docker compose up --build -d  # для Docker Compose
# или следуйте "Способу 2" для Node.js напрямую
```

## Проверка что всё работает

```bash
# 1. Проверьте что контейнеры/процессы запущены
docker compose ps
# или
pm2 list

# 2. Проверьте логи на ошибки
docker compose logs -f server  # Docker
# или
pm2 logs neko-finance  # PM2

# 3. Откройте в браузере
# Перейдите по URL вашего сервера, например:
# https://yourdomain.com
# http://your-server-ip:3000

# 4. Проверьте что API работает
curl http://localhost:3000/accounts  # должен вернуть JSON
```

## Что изменилось в этой версии

✅ **Новые функции:**
- Система счетов (accounts)
- Копилки/цели накопления (savings goals)
- Возможность добавлять расходы на конкретный счет или в копилку
- Копилки отображаются в списке счетов на главном экране

✅ **Улучшения:**
- Мобильный дизайн оптимизирован
- Убран баланс из названия счета (экономим место)
- Убрана опция "Без счета" - теперь нужно выбрать счет/копилку обязательно
- Добавлено логирование для отладки

✅ **Важно:**
- База данных автоматически создаст таблицы `accounts`, `savings_goals`, `transfers` если их нет
- Старые данные о транзакциях сохранятся
- Бюджеты и лимиты работают так же как раньше

## Если что-то пошло не так

```bash
# 1. Посмотрите логи
docker compose logs server  # для Docker

# 2. Проверьте что порты не заняты
netstat -tlnp | grep :3000  # Linux
netstat -ano | findstr :3000  # Windows

# 3. Перезагрузитесь полностью
docker compose down && docker compose up --build -d

# 4. Очистите кэш браузера
# Нажмите Ctrl+Shift+Delete в браузере и очистите всё

# 5. Если всё ещё не работает - проверьте .env файлы (если есть)
# и убедитесь что все переменные окружения установлены правильно
```

## Откат на старую версию (если что-то сломалось)

```bash
# На сервере:

# 1. Посмотрите историю коммитов
git log --oneline | head -5

# 2. Вернитесь к старой версии
git checkout <commit-hash>  # например: git checkout a1b2c3d

# 3. Перезагрузитесь
docker compose up --build -d
# или
npm start  # для Node.js напрямую
```

---

**Если у вас остались вопросы - пишите в issues на GitHub!** 🎉
